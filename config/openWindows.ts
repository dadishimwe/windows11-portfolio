export type OpenWindows = {
	fileExplorer: boolean;
	notepad: boolean;
	terminal: boolean;
	firefox: boolean;
};

export const initialOpenWindows: OpenWindows = {
	fileExplorer: false,
	notepad: false,
	terminal: false,
	firefox: false,
};
